# ClinicalEdge — Pharma Competitive Intelligence Agent
# Kaggle Capstone · 5-Day AI Agents Intensive · Google ADK + MCP
# All packages are pip-installable on Kaggle (Python 3.10+, internet enabled)

# ── Core ADK & MCP ──────────────────────────────────────────────────────────
google-adk>=1.0.0              # Google Agent Development Kit
mcp>=1.0.0                     # Model Context Protocol Python SDK

# ── HTTP / Async ─────────────────────────────────────────────────────────────
httpx>=0.27.0                  # Async HTTP client with retry support
aiohttp>=3.9.0                 # Alternative async HTTP (used in MCP servers)
tenacity>=8.3.0                # Retry with exponential back-off

# ── Data Handling ────────────────────────────────────────────────────────────
pydantic>=2.7.0                # Validated data models for structured outputs
python-dateutil>=2.9.0         # Date parsing for trial/paper timestamps

# ── Display / Formatting ─────────────────────────────────────────────────────
rich>=13.7.0                   # Rich terminal output for demo section
tabulate>=0.9.0                # ASCII tables in notebook sections

# ── Security ─────────────────────────────────────────────────────────────────
presidio-analyzer>=2.2.354     # Microsoft Presidio — PHI / PII detection
spacy>=3.7.0                   # NLP backbone required by Presidio
# NOTE: after install run:  python -m spacy download en_core_web_sm

# ── Google AI ────────────────────────────────────────────────────────────────
google-genai>=0.8.0            # New Google Gen AI SDK (replaces deprecated google-generativeai)
